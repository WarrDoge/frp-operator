apiVersion: v1
kind: Secret
metadata:
  name: edge-secret
  namespace: default
type: Opaque
stringData:
  # Replace with your FRP server token. The same key is reused as the
  # loadBalancer.groupKey on the Upstream so the DaemonSet members share
  # group membership at the FRP layer.
  token: your-secret-token
